import { CalcKarkerdPipe } from './calc-karkerd.pipe';

describe('CalcKarkerdPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcKarkerdPipe();
    expect(pipe).toBeTruthy();
  });
});
