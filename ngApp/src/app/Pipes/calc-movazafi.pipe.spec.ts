import { CalcMovazafiPipe } from './calc-movazafi.pipe';

describe('CalcMovazafiPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcMovazafiPipe();
    expect(pipe).toBeTruthy();
  });
});
