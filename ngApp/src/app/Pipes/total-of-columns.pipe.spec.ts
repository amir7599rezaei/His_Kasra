import { TotalOfColumnsPipe } from './total-of-columns.pipe';

describe('TotalOfColumnsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalOfColumnsPipe();
    expect(pipe).toBeTruthy();
  });
});
